import {KHA_PATH, CURRENT_PATH, ENGINE_PATH} from "./const";
import {run} from './args';

console.log(`Current: ${CURRENT_PATH}`);
console.log(`Engine: ${ENGINE_PATH}`);
console.log(`Kha: ${KHA_PATH}`);

run();

